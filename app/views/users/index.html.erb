<main>
  <div class="container px-5 px-sm-0">
    <div class="row">
      <%= render 'books/list', book:@book, user:current_user %>
      <div class="col-md-8 offset-md-1">
        <h2>Users</h2>
        <table class="table">
          <thead>
            <tr>
              <th>image</th>
              <th>name</th>
              <th colspan="3"></th>
            </tr>
          </thead>
          <tbody>
            <% @users.each do |user|%>
              <tr>
                <td><%= attachment_image_tag user, :profile_image, :fill,40,40, format:'jpeg', fallback: 'no-image.jpeg'%></td>
                <td><%= user.name%></td>
                <% if current_user.id != user.id%>
                  <td id="user_index_relationship_<%= user.id%>">
                    <%= render 'relationships/relationship_user', user: user %>
                  </td>
                <% else %>
                  <td></td>
                <% end %>
                <td><%= link_to 'Show', user_path(user.id)%></td>
              </tr>
            <% end %>
          </tbody>
        </table>
      </div>
    </div>
  </div>
</main>